use 'conduit_db' as db

pub type BlogPost {
    rel .'id': { rel .is: uuid }
    rel .'body': string
}

map => {
    db.Article {
        'article_id': id
        'body': body
    }
    BlogPost {
        // TODO: fix this ID conversion
        'id': id // ERROR cannot convert this `uuid` from `uuid`: These types are not equated.
        'body': body
    }
}
