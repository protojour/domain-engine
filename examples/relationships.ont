# Experimenting with a very vague nomenclature..
# Also demonstrating a model that develops from vague into more concrete over time.
# Trying to express relationships between concepts

(entity guitar)
(entity string)
(entity knob)

# demonstrating the slight ambiguity between an attribute and a relationship.
# Two concept related by a "has" relationship is very close to the first having some "attribute".
(has! guitar (array string 6))
(has! guitar knob)
