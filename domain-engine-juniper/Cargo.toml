[package]
name = "domain-engine-juniper"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
doctest = false

[dependencies]
ontol-runtime.path = "../ontol-runtime"
smartstring = { version = "1", features = ["serde"] }
serde = "1"
juniper = { git = "https://github.com/protojour/juniper", branch = "protojour-mods" }
indexmap = { version = "1", features = ["serde"] }
serde_json = "1"
tracing = "0.1"
fnv = "1"

[dev-dependencies]
ontol-test-utils.path = "../ontol-test-utils"
tracing-subscriber = {version = "0.3", default-features = false, features = ["env-filter", "fmt"]}
test-log = { version = "0.2", default-features = false, features = ["trace"] }
tokio = { version = "1", features = ["macros", "rt"] }
async-trait = "0.1"
pretty_assertions = "1"
