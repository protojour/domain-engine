def foo (rel .'a': {text})
def bar (rel .'b': {text})

map(
    foo(
        'a': x // ERROR {`ontol.text`} variable must be enclosed in {}
    ),
    bar(
        'b': x // ERROR {`ontol.text`} variable must be enclosed in {}
    ),
)
