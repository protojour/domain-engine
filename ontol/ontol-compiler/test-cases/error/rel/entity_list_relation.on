def foo(
    rel. 'id': (rel* is: text)
    rel* 'bars': [bar] // ERROR entity relationships must be sets instead of lists. Use `{}` syntax.
)
def bar(
    rel. 'id': (rel* is: text)
    rel* 'field': text
)
