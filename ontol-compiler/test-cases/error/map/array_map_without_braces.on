def foo (rel .'a': {text})
def bar (rel .'b': {text})

map(
    foo(
        'a': x // ERROR {text} variable must be enclosed in {}
    ),
    bar(
        'b': x // ERROR {text} variable must be enclosed in {}
    ),
)
